# HexedNET

HexedNET is a fork of [WSUTComp](https://github.com/zenakuten/WSUTComp) that extracts the enhanced netcode from the myriad of features inside UTComp. Everything else was removed.
Releases are bundled together with HexedUT (see [HexedUT2k4](https://github.com/hexengraf/HexedUT2k4)).
